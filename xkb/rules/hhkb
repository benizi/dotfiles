! model = keycodes types compat
  * = evdev+hhkb complete hhkb

! model = symbols
  * = pc+inet(evdev)+%l(%v)

! model = geometry
  * = pc(%m)

! option = symbols
  terminate:ctrl_alt_bksp = +terminate(ctrl_alt_bksp)
