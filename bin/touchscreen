#!/bin/sh
output=$(xrandr | awk '/ connected/ { print $1 ; exit }')

map() {
  for input
  do xinput map-to-output $input $output
  done
}

map $(xinput list | awk -F'[\\t=]' '/Touchscreen|PenTablet/ && /pointer/ { print $3 }')
